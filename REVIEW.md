# Review instructions

本文件是 `report.md` §8.5 给出的模板在本仓库上的实例。本仓库的评审必须符合本仓库自己得出的结论——若这里的规则与报告的建议冲突，那是缺陷，不是例外。

## Important 在本仓库的定义

仅当满足以下之一才标 🔴 Important：

- 正文中的某个数字与 `derived/stats.json` 不一致，或无法由 `raw/` 重算得到
- 引用了 `lit/quotes.md` 中未登记的文献数值
- 某个结论的强度超出其证据所能支持的范围（例如把上界当点估计、把聚簇样本当独立观测、把相关当因果）
- 脚本无法从落盘数据离线重跑，或重跑结果与已提交产物不一致
- 图表标题或说明与它所依据的数据相矛盾
- 出现了未标注方向的口径偏差

行文风格、章节顺序、措辞偏好、图表配色的个人品味，一律最高 🟡 Nit。

## Verification bar

事实断言必须给出可核对的位置：`derived/stats.json` 的键名、`raw/` 的文件与字段、`lit/quotes.md` 的小节号，或文档 URL。**不接受从措辞或命名推断出的断言。** 拿不出位置的发现降级为 Nit 或不报。

报告一个数字错误之前先自问：能否给出 `scripts/verify.py` 中对应的失败断言，或一条能复现该不一致的命令？不能就不报。

## Re-review convergence

如果本文件所在的变更已经被评审过：

- 只报本轮新增内容引入的问题，不重新审视上一轮已通过的章节
- 抑制所有新的 Nit，只报 🔴 Important
- 不得重提任何已被明确拒绝并给出理由的发现

## Nit 上限

每次评审最多 5 条 🟡。超出的在 summary 里写 "plus N similar items"。如果全部发现都是 Nit，summary 第一行写 "No blocking issues."

## 不要报告

- `scripts/verify.py` 已覆盖的一致性问题——让 verifier 报，不要重复
- `raw/` 下的内容：那是 API 原始响应，不得为了美观而修改
- `lit/quotes.md` 中引文的措辞：那是原文逐字摘录，不得改写
- 自然段内没有硬换行这一写作约定（这是有意为之）

## Summary 形状

第一行给计数：`N important, M nit`。

## 终止

轮数上限 3。连续 2 轮阻塞级发现数未减少即停止并升级。未处理的非阻塞项移入 `report.md` §9.3 的 follow-up 清单，不得留在正文里悬置，也不构成新增一轮的理由。
